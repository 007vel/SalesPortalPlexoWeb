/**
 * Production environment values. Assumes the built app is served from the
 * same origin as the PWR API; point this at an absolute URL instead if it's
 * hosted elsewhere.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api',
};
